# Part 5 — Markdown skill tool

Run `python agent.py`. This lesson retains Part 3's retrieval and MCP weather behavior. It deliberately has no summary subagent: for a summary the main agent calls `load_executive_brief_skill`, reads `SKILL.md`, and applies those instructions itself.
