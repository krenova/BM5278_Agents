# Part 2 — Model-selected RAG tool

Install `-r requirements.txt`, configure `.env`, then run `python agent.py`. Unlike Part 1, the model calls `search_course_notes` only when it considers the notes useful; each call is printed.
