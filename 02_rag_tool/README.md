# Part 2 — Model-selected RAG tool

Install the root `requirements.txt` and configure the single root `.env`, then run `python agent.py` from this folder. Unlike Part 1, the model calls `search_course_notes` only when it considers the notes useful; each call is printed.
